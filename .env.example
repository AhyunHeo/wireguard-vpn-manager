# WireGuard VPN Manager 환경변수 설정

# 서버 공인 IP 또는 도메인
# auto로 설정하면 자동 감지
SERVERURL=auto

# API 인증 토큰 (강력한 랜덤 문자열 사용)
# 생성: openssl rand -hex 32
API_TOKEN=change-this-to-secure-token

# PostgreSQL 설정
DB_HOST=postgres  # Docker Compose 서비스명
DB_PORT=5432
DB_USER=vpn
DB_PASSWORD=vpnpass
DB_NAME=vpndb

# WireGuard 설정
WG_INTERFACE=wg0
WG_PORT=51820
WG_SUBNET=10.100.0.0/16

# API 서버 설정
API_PORT=8090

# 타임존
TZ=Asia/Seoul